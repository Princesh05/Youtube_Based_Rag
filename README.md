# Youtube_Based_Rag
Developed a Retrieval-Augmented Generation (RAG) application using LangChain that retrieves YouTube transcript data, splits it into meaningful chunks, generates embeddings, and stores them in a FAISS vector database. The system uses semantic retrieval and a Google Gemini LLM to provide context-aware answers based on the retrieved information.
